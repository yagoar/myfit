# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
.claude/

# Project data (gitignored per SPEC.md Section 8)
data/body_models/
data/vposer/
data/captures/
data/results/
data/smplx_female_tpose.obj

# Video-ingest artefacts: keep transcripts and frames (citable source),
# drop the heavy bits. Source videos are either symlinks into the user's own
# filesystem or yt-dlp downloads; audio is regenerable from either.
references/**/audio.wav
references/**/source.*

# Whisper model cache (per-machine, not part of repo)
.whisper-models/

# Generated review page (regenerable from merged.yaml)
review.html
